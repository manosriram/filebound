import React, { Fragment } from "react";

const Progress = ({ percentage }) => {
    return (
        <Fragment>
            <h3>{percentage}</h3>
        </Fragment>
    );
};


export default Progress;
